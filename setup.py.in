from setuptools import setup, find_packages

setup(name='@PACKAGE@', version='@VERSION@',
      packages=find_packages('src/python'), package_dir={'': 'src/python'},
      test_suite='kryten.test.testsuite')
